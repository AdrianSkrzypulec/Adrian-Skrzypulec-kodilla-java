
SELECT I.NAME, L.NAME
FROM ISSUESS I
         JOIN ISSUESLISTS L; 

SELECT I.NAME, U.FIRSTNAME, U.LASTNAME
FROM ISSUESS I
         JOIN USERS U;

SELECT U.FIRSTNAME, U.LASTNAME,COUNT(*) AS ISSUESS_NUMBER
FROM ISSUESS
JOIN USERS U
GROUP BY U.LASTNAME, U.FIRSTNAME; # lub U.ID

